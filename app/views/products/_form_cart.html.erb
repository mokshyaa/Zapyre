<%= form_with(model: @cart ,url: carts_path, method: "post", local: true) do |form| %>
	<%= form.hidden_field :product_id, value: product_id  %>
 	<%= render partial: "products/form_quantity" , locals: {product_id: product_id} %> 
<% end %>
		
	