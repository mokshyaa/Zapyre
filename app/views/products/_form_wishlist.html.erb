<div class="row">	
	<div class="col-md-6"> 
		<%= form_with(url: wishlists_path(wishlist: @wishlist), method: "post", local: true) do |form| %>
			<p><%= form.hidden_field :product_id, value: product_id  %></p>
			<p><%= form.submit 'Wishlist',class: 'btn btn-primary',  data: { "disable-with": "Wishlisted"}%></p>
		<% end %>
	</div>
</div>


