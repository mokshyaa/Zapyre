
  <div class="row"> 
	  <%= form_with(model:  @wishlist,  url: wishlists_path, method: "post", local: true) do |form| %>
	    <p>
	      <%= form.hidden_field :product_id, value: product.id  %>
	   </p>
	   <p>
	      <%= form.submit 'Wishlist',class: 'btn btn-primary'%>
	   </p>
	   <% end %>
  </div>

